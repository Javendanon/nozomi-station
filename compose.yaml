services:
  liquidsoap:
    image: savonet/liquidsoap:v2.4.5
    user: "${LIQUIDSOAP_UID:-100}:${LIQUIDSOAP_GID:-101}"
    init: true
    restart: unless-stopped
    command: /etc/liquidsoap/radio.liq
    volumes:
      - ./config/liquidsoap:/etc/liquidsoap:ro
      - ./priv/static/hls:/hls
